---
name: auto-reply
description: 自动回复生成器 - 根据模式生成自动回复，节省重复性回复时间
metadata:
  {
    "openclaw":
      {
        "emoji": "💬",
        "requires": {
          "bins": ["bash"]
        }
      }
  }
---

# Auto Reply 💬

自动回复生成器，根据预定义模式生成回复，节省重复性回复时间。

## 概述

`auto-reply` 让你可以：
- 定义回复模式模板
- 根据关键词生成回复
- 管理多个回复规则
- 快速生成常用回复

## 设置

### 前置要求
- Bash

### 安装
```bash
cd ~/clawd
mkdir -p scripts
cp [path/to]/auto-reply.sh scripts/
chmod +x scripts/auto-reply.sh
mkdir -p memory/reply-templates
```

## 使用方法

### 生成回复
```bash
./scripts/auto-reply.sh "关键词"
```

### 定义新模板
编辑回复模板文件（通常在 `memory/reply-templates/`）：
```json
{
  "你好": "你好！有什么我可以帮助你的吗？",
  "再见": "再见！祝你一切顺利！",
  "感谢": "不客气！很高兴能帮到你！"
}
```

## 常用模板

### 问候
```json
{
  "greeting": "你好！我是{assistant_name}，今天有什么我可以帮助你的？"
}
```

### 确认
```json
{
  "confirm": "明白了，我会{action}。"
}
```

### 拒绝
```json
{
  "decline": "抱歉，我无法完成这个请求。{reason}"
}
```

## 使用场景

### 场景 1: 问候回复
```bash
./scripts/auto-reply.sh "你好"
```

### 场景 2: 确认操作
```bash
./scripts/auto-reply.sh "确认文件删除"
```

### 场景 3: 礼貌拒绝
```bash
./scripts/auto-reply.sh "抱歉无法访问"
```

## 仓库

https://github.com/90le/openclaw-skills-hub

---

**回复更快！** 💬
